/*
 * Operators: -
 * ---------
 *      1. Arithematic  -> +    -   *   /   %
 *      2. Assignment   -> +=   -=  *=  /=  %=
 *      3. Comparison   -> <    >   <=  >=  ==  !=
 *      4. Logical      -> &&   ||  !
 *
 * Flow Control: -
 * ------------
 *      1. if (condition) { // code }
 *      2. if (condition) { // code } else if (condition) { // code }
 *      3. if (condition) { // code } else if (condition) { // code } else { // code }
 *      4. loop { // code }
 *      5. while (condition) { // code }
 *      6. for variable in start..end { // code }
 *      7. break        - terminates the loop
 *         continue     - skips the current iteration of the loop and moves on to the next
 *      8. match value { pattern1 => expr1, pattern2 => expr2, ... , patternN => exprN, _ => default expr }
 *
 * */
